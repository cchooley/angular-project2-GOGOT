export interface Character {
    name: string,
    gender: string,
    born: string,
    titles: Array<string>,
    aliases: Array<string>,
    playedBy: string,
    pickedBy: string,
    timesPicked: number,
    pickedAt: string
}
